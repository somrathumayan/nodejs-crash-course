const os = require("os");

// platform 

console.log(os.platform());

// CPU Architecture 
console.log(os.arch());

// CPU Core Info 
console.log(os.cpus());

// FRee memory 
console.log(os.freemem());

// total memory
console.log(os.totalmem());